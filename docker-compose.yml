version: "3.9"

services:
  face-server:
    build: .
    ports:
      - "8001:8001"
    volumes:
      - ./known_faces:/app/known_faces   # hot-reload faces without rebuild
      - ./logs:/app/logs                 # intruder logs saved to host
    restart: unless-stopped